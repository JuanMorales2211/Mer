))
contador = 2
while ( contador <= numero_primos):
    numero
    divisor = 2
    while ( divisor <= numero):
        residuo= numero % divisor 
        if ( residuo == 0) :
            if (divisor < numero) :
                divisor = numero +1
            else:
                print ( 'es primo', numero )
                divisor = numero +1
        divisor += 1


